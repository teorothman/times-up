<div class="rules-wrapper">
  <div class="rule" data-name="rule-1">
    <div class="page-1 page-1-on-click">
      <h1 class="rules-title"><%= Rule.all.first.title %></h1>
    </div>
    <div class="page-1 page-1-on-click">
      <p class="rule-text"><%= Rule.all.first.content %></p>
      <div class="btn-wrapper">
      <div class="btn-rules">
        <%= button_tag class:"btn-red", data: { name: "rule-1", action: "click->show-rules#next" } do %>
          <i class="fa-solid fa-right-long"></i>
          <h3>NEXT</h3>
        <% end %>
      </div>
      </div>
    </div>
  </div>

  <div class="rule hidden" data-name="rule-2">
    <div class="page-2 page-2-on-click">
      <h1 class="rules-title"><%= Rule.all.second.title %></h1>
    </div>
    <div class="page-2 page-2-on-click">
      <p class="rule-text"><%= Rule.all.second.content %></p>
      <div class="btn-wrapper">
      <div class="btn-rules">
        <%= button_tag data: { name: "rule-2", action: "click->show-rules#back" } do %>
          <i class="fa-solid fa-left-long"></i>
          <h3>BACK</h3>
        <% end %>
      </div>
      <div class="btn-rules">
        <%= button_tag data: { name: "rule-2", action: "click->show-rules#next" } do %>
          <i class="fa-solid fa-right-long"></i>
          <h3>NEXT</h3>
        <% end %>
      </div>
      </div>
    </div>
  </div>

  <div class="rule hidden" data-name="rule-3">
    <div class="page-3 page-3-on-click">
      <h1 class="rules-title"><%= Rule.all.third.title %></h1>
    </div>
    <div class="page-3 page-3-on-click">
      <p class="rule-text"><%= Rule.all.third.content %></p>
      <div class="btn-wrapper">
      <div class="btn-rules">
        <%= button_tag data: { name: "rule-3", action: "click->show-rules#back" } do %>
          <i class="fa-solid fa-left-long"></i>
          <h3>BACK</h3>
        <% end %>
      </div>
      <div class="btn-rules">
        <%= button_tag data: { name: "rule-3", action: "click->show-rules#next" } do %>
          <i class="fa-solid fa-right-long"></i>
          <h3>NEXT</h3>
        <% end %>
      </div>
      </div>
    </div>
  </div>

  <div class="rule hidden" data-name="rule-4">
    <div class="page-4 page-4-on-click">
      <h1 class="rules-title"><%= Rule.all.last.title %></h1>
    </div>
    <div class="page-4 page-4-on-click">
      <p class="rule-text"><%= Rule.all.last.content %></p>
      <div class="btn-wrapper">
      <div class="btn-rules">
        <%= button_tag data: { name: "rule-4", action: "click->show-rules#back" } do %>
          <i class="fa-solid fa-left-long"></i>
          <h3>BACK</h3>
        <% end %>
      </div>
      <div class="btn-play">
        <%= button_tag data: { action: "click->show-rules#hide" } do %>
          <i class="fa-solid fa-gamepad"></i>
          <h3>PLAY</h3>
        <% end %>
      </div>
      </div>
      </div>
    </div>
  </div>
